function reverseWordsInString(string) {
  let newString = "";
  for (let i = str.length - 1; i >= 0; i--) {
    newString += str[i];
  }
  return newString;
}
const string = "string";
const result = reverseString(string);
